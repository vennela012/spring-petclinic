pipeline{
    agent any
    stages{
        stage ('vcs'){
            steps{
                git branch: 'vens' 
                git url:
            }
            stage ('build'){
                steps{
                    sh "mvn package"
                }
                stage('archive results'){
                    steps{
                        junits '**/surefile-reports/*.xml'
                        

                    }
                }

            }
        }
    }
}